import { AcotarStringPipe } from './acotar-string.pipe';

describe('AcotarStringPipe', () => {
  it('create an instance', () => {
    const pipe = new AcotarStringPipe();
    expect(pipe).toBeTruthy();
  });
});

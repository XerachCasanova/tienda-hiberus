import { DescripcionPipe } from './descripcion.pipe';

describe('DescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new DescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
